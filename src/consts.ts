export const defaultAssistantId = 'DEFAULT';

export const defaultApiHost = "https://api.impaction.ai";

export const customPropertyKeyPattern = "^[a-zA-Z][a-zA-Z0-9_]{0,63}$";
export const customPropertyKeyRegex = new RegExp(customPropertyKeyPattern);